# Frontend é HTML/CSS/JS puro — não precisa de build, só de um servidor
# que sirva os arquivos estáticos. Nginx é leve e é o padrão pra isso.
FROM nginx:1.27-alpine
COPY . /usr/share/nginx/html
EXPOSE 80
